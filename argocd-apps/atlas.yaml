apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: atlas
  namespace: argocd
  annotations:
    description: "Atlas resources pour la gestion des migrations de base de données"
spec:
  project: default
  source:
    path: apps/atlas/
    repoURL: "https://github.com/naqa92/portfolio-ultime-config.git"
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: "demo"
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
